OHGS WEBSITE — NEW VERCEL PROJECT AND DOMAIN

1. First upload this package to the ROOT of the correct GitHub repository.
2. In Vercel choose Add New > Project, then import that GitHub repository.
3. Select the production branch that contains the upload (normally main).
4. Set Framework Preset to Other.
5. Set Root Directory to ./ (the repository root).
6. Set Build Command to: npm run build
   Set Output Directory to: dist
   These settings are also included in vercel.json so Vercel publishes the checked website files, not an older build.
7. Deploy without the previous build cache. Open /version-check.txt and confirm it says v23-award-video-home.
8. In the NEW project open Settings > Domains and add your domain.
9. If Vercel says the domain belongs to the old project, remove it from the old project's Domains page, then add it here.
10. At your domain provider, enter exactly the DNS records Vercel displays. Do not guess the records.
11. In Vercel Settings > Git, confirm the Production Branch is the branch you uploaded.

LIVE VERSION CHECK
Your deployed-domain/version-check.txt must show:
OHGS RELEASE v23-award-video-home — Home begins with a large autoplay showroom tour plus two immediately visible playable video previews; all nine videos remain in Gallery
